<div class="comments-name">
  <p>Poster par <%= User.find(user_id).first_name %>
    <% if current_user.id == user_id %>
        <%= link_to "supprimer", video_comment_path(@video,comment), method: :delete, data: { confirm: "Êtes-vous sûr?" } %>
    <% end %>
  </p>
  <div class="comments-content">
    <p><%= comment.content %></p>
  </div>
</div>
